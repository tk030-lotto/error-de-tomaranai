# コードレビュー報告書（品質監査）

**プロジェクト名**: エラーで止まらない。ツール
**実施日**: 2026-08-25
**監査種別**: コードレビュー（`quality-audit.md` 横断的品質監査フレームワーク準拠）
**監査方法**: 全行静的精読・仕様照合・セキュリティ監査・アクセシビリティ監査・資料間矛盾チェック
**総合判定**: **REQUIRES FIX（要修正）**

> **制約事項**: 本セッションでは MCP監査ツール（第22条）/ reviewerエージェント（第23条）/ シェル実行（node構文チェック等）が利用できなかったため、手動全行精読による静的解析で代替した。ブラウザでの実機動作テストは未実施。

---

## 1. 監査結果サマリー

| ID | 分類 | 重要度 | 対象ファイル・箇所 | 問題内容 | 推奨対応 | 修正要否 |
|---|---|---|---|---|---|---|
| **QA-001** | プロトコル違反(第17条) | **High** | `css/components.css` | 総行数 **336行** で300行上限超過（前回指摘のまま未修正）。※前回報告の「337行」は実測では336行 | 用途別へのファイル分割 | **必須** |
| **QA-002** | UI/UXバグ | **Medium** | `css/components.css:110` | `input`/`textarea` の `font-size: 0.95rem` により iOS Safari フォーカス時に画面自動ズーム（前回指摘のまま未修正） | `font-size: 1rem;` へ変更 | **必須** |
| **QA-005** | アクセシビリティ | **Medium** | `css/tokens.css:20` | `--text-muted: #71717a` の `#121215` 上コントラスト比 **約3.9:1**（WCAG AA 4.5:1 未満）。小サイズ実文字に使用中 | `#8b8b96` 等への引き上げ | 推奨 |
| **QA-006** | 資料間矛盾 | **Medium** | `README.md` / `index.html` | 「外部サーバーやライブラリに一切依存せず」記述と Google Fonts CDN 読み込みの矛盾 | README文言修正等 | 推奨 |
| **QA-007** | ドキュメント欠陥 | Low | `仕様書.md:328行目以降` | 仕様と無関係なAI対話ログ混入・閉じフェンス破損・空コードブロック | 末尾削除と修復 | 推奨 |
| **QA-008** | 記録不整合 | Low | `RECORD.md` / `audit_report*.md` | `mcp_config.json`・`copilot-instructions.md` が実在しない／監査レポート2本が同一内容の重複 | RECORD是正・レポート統合 | 推奨 |
| **QA-009** | アクセシビリティ | Low | `index.html` / `app.js` | `aria-expanded`/`aria-controls`/`role="status"`/`role="tab"` 等のARIA属性未設定 | ARIA属性付与 | 任意 |
| **QA-010** | アクセシビリティ | Low | `index.html:111,115,119,175` | label要素が `for` 属性で未紐付け／`modifyInput` にラベルなし | `for`/`id` 付与 | 任意 |
| **QA-011** | 堅牢性 | Low | `app.js:220-234` | クリップボードフォールバックで catch がエラー握り潰し・`execCommand('copy')` 戻り値未検証 | `console.warn` 追加・戻り値検証 | 任意 |
| **QA-012** | UX一貫性 | Low | `app.js:237-252` | `resetAll()` でスタイル選択（タブactive含む）が「標準」に戻らない | 初期化処理追加（意図的なら対応不要） | 要確認 |
| **QA-013** | UX | Low | `app.js:56-67` | エラーチップ連打で同一メッセージが改行付きで重複追記される | 重複時スキップ等 | 任意 |

---

## 2. 重大問題詳細

### QA-001【プロトコル違反 / High】css/components.css の行数上限超過
- **対象**: `css/components.css`（実測 **336行**、最終行336）
- **根拠**: プロトコル第17条「ファイルが300行を超えたらユーザーへ分割を提案すること」。
- **補足**: 前回監査レポート（`AUDIT_REPORT_2026-08-25.md`）は「337行」と記載しているが、実測は336行。超過事実自体に変わりはないが、記録の数値は是正対象。
- **推奨対応**: 用途別分割（例: `css/forms.css`（カード/チップ/入力/ボタン系）と `css/overlays.css`（タブ/アコーディオン/トースト系））。分割後は `index.html` のlinkタグ追記が必要。
- **修正必須**: はい（ユーザー承認が必要な構造変更）

### QA-002【UI/UXバグ / Medium】iOS Safari のフォーカス自動ズーム
- **対象**: `css/components.css` **110行目** `font-size: 0.95rem;`
- **根拠**: iOS Safari はフォントサイズ16px未満の入力欄フォーカス時にビューポートを自動ズームする仕様を持つ。
- **発生条件**: iOS端末 + Safari でエラー入力欄等をタップ。
- **影響**: 仕様書 §3「スマートフォンを基本とする」に照らすと主要ターゲットでの入力体験を損なう実害。
- **推奨対応**: `font-size: 1rem;` へ変更（**1行修正**）。
- **修正必須**: はい

---

## 3. 今回新規検出の詳細

### QA-005【アクセシビリティ / Medium】muted色のコントラスト比不足
- **対象**: `css/tokens.css` 20行目 `--text-muted: #71717a`
- **根拠**: 背景カード `#121215` 上の相対輝度から算出したコントラスト比は **約3.9:1**。WCAG 2.1 AA基準（通常テキスト 4.5:1）を下回る。
- **影響箇所**: `.preset-label`（0.8rem）、`.char-counter`、`.next-badge`、`.next-desc` 等の小サイズ実文字。placeholder は実文字ではないが同色を使用。
- **推奨対応**: `#8b8b96` 程度への引き上げで 4.5:1 を満たす見込み（変更後は実機での最終確認を推奨）。

### QA-006【資料間矛盾 / Medium】READMEの依存ゼロ宣言とWebフォントCDNの矛盾
- **対象**: `README.md` 40行目 vs `index.html` 8〜10行目
- **根拠**: READMEは「外部サーバーやライブラリに一切依存せず」と記載。一方、実装は Google Fonts（fonts.googleapis.com / fonts.gstatic.com）からフォントを取得しており「外部サーバー」に依存している。
- **影響**: 機能影響なし（フォント取得失敗時はシステムフォントへフォールバック）。ただし資料と実装の不一致であり、監査基準 Step 1「資料間の矛盾は問題として記録」に該当。
- **推奨対応**: README文言修正（例: 「JS/CSSライブラリ・APIサーバー非依存。WebフォントのみCDN利用」）が最小対応。完全準拠する場合はフォントの自己ホスト。

### QA-007【ドキュメント欠陥 / Low】仕様書.md への対話ログ混入
- **対象**: `仕様書.md` 328行目以降
- **根拠**: 仕様本文の末尾に「### ここまでで4本 流れがかなり綺麗になってきました…」という仕様と無関係なAI対話ログが混入。閉じコードフェンスが破損（4連バッククォート）。§20 ライセンス節に空コードブロックあり。
- **推奨対応**: 328行目以降を削除し、フェンスを修復。

### QA-008【記録不整合 / Low】RECORD.md の主張と実ファイルの不一致・レポート重複
- **根拠**:
  1. RECORD.md が同梱を主張する `mcp_config.json`、`copilot-instructions.md` が実在しない（存在確認済み）。
  2. `audit_report.md` と `AUDIT_REPORT_2026-08-25.md` が同一内容の重複ファイル。更新時に片方のみ修正されるドリフトリスク。
- **推奨対応**: RECORD.md 該当行の是正。監査レポートは日付付き1本に統合（本ファイルを最新正本とする）。

### QA-009【アクセシビリティ / Low】WAI-ARIA属性の未設定
- **対象**: アコーディオン（`contextToggleBtn`/`modifyToggleBtn`）、トースト（`toast`）、スタイルタブ（`.tab-btn`）
- **根拠**:
  1. アコーディオンボタンに `aria-expanded` / `aria-controls` なし → 開閉状態が支援技術に伝わらない。
  2. トーストに `role="status"`（aria-live）なし → 「コピーしました」等が読み上げされない。
  3. スタイルタブに `role="tablist"` / `role="tab"` / `aria-selected` なし。
- **推奨対応**: `app.js` のトグル処理内で `aria-expanded` を同期更新し、トーストに `role="status"` を付与。

### QA-010【アクセシビリティ / Low】label要素の紐付け不足
- **対象**: `index.html` 111 / 115 / 119 行目のlabel、175行目 `modifyInput`
- **根拠**: labelが `for` 属性で入力欄と紐付けされていない（クリック領域・読み上げの両面で不利益）。`modifyInput` にはラベル自体がなく placeholder のみ。
- **推奨対応**: `for`/`id` の付与、`modifyInput` 用の視覚ラベル（または `aria-label`）追加。

### QA-011【堅牢性 / Low】クリップボードフォールバックのエラー握り潰し
- **対象**: `app.js` 220〜234行目
- **根拠**: catch節でエラーを記録せず握り潰している（監査基準 Step 4「エラーを握り潰していないか」に抵触）。`document.execCommand('copy')` の戻り値を検証せず成功トーストを表示するため、非HTTPS環境・iOSのasyncコンテキスト等でコピー失敗時に誤った成功通知が出る可能性。
- **推奨対応**: `console.warn(err)` の追加、`execCommand` 戻り値分岐と失敗時トーストの表示。

### QA-012【UX一貫性 / Low】リセット時にスタイル選択が初期化されない
- **対象**: `app.js` 237〜252行目 `resetAll()`
- **根拠**: 入力欄・状況チップ・修正要望は初期化されるが、`currentStyle` とタブのactive表示は選択されたまま。仕様上の要求はないため**意図的設計の可能性あり（確認事項）**。
- **推奨対応**: 初期化する場合は `currentStyle = 'standard'` へ戻し、タブactiveも先頭へ移動。

### QA-013【UX / Low】エラーチップ連打による重複追記
- **対象**: `app.js` 56〜67行目
- **根拠**: 同一チップを複数回クリックすると同じメッセージが `\n` 付きで重複追記される。仕様 §6 は自由入力前提のため軽微。
- **推奨対応**: 既存値に含まれる場合は追記しない等のガード（任意）。

---

## 4. セキュリティ監査: ✅ 合格（PASS）

| 項目 | 判定 | 根拠 |
|---|---|---|
| XSS | 合格 | DOM出力は `textContent` / `value` のみ。`innerHTML` 等は不使用（全行確認済み） |
| 秘密情報 | 合格 | APIキー等のハードコードなし。`mcp_config.json` は存在しないため漏洩リスクなし |
| 外部通信 | 合格 | JSからのAPI呼び出しなし（仕様書 §15「AI API不使用」準拠）。外部取得はWebフォントのみ |
| リンク安全策 | 合格 | 外部リンクすべて `rel="noopener noreferrer"` 付き |
| .gitignore | 合格 | 大容量メディア除外 + `!demo.gif` 例外の適切な設定 |
| 自動実行 | 合格 | 仕様書 §16 準拠。本ツール自身がコード・コマンドを自動実行する処理なし |

## 5. 仕様照合（仕様書.md vs 実装）

**照合結果: 実装済み（合格）**
- §5 起動画面 / §6 エラー入力 / §7 状況入力 / §8 補足入力 / §9〜10 質問生成内容（AI依頼6項目を標準スタイルで網羅）/ §11 表示と操作 / §12 コピー（文言「コピーしました。AIに送ってみましょう！」一致）/ §13 修正（仕様の例チップ3種を完全再現）/ §14 AI非依存 / §15 API不使用 / §16 安全設計 / §17 実装しない機能の非混入 / §19 完成条件12項目

**軽微な不一致（確認事項）**
- 仕様書 §8 入力例「実際に起きたこと」および §9「実際に発生した動作」に対応する入力欄・質問文項目が未実装。
- §8 は「入力例」表記のため必須要件とは断定しない。対応する場合は追加実装または仕様書側の明記が必要。

## 6. テスト不足

- **Low**: 自動テストスイート（Jest/Vitest等）なし。Zero-Dependency の Vanilla ツールという性質上許容範囲（前回監査 QA-004 の判定「任意」を妥当と確認）。
- **推奨**: 手動確認チェックリストの整備（空入力→警告トースト／生成→コピー→再生成→リセットの一連経路／モバイル幅での表示）。

## 7. 追加機能提案

なし（監査基準に従い、仕様外の機能要求は本報告では行わない）。

---

## 8. 修正優先順位

1. **QA-002**（1行修正: `font-size: 1rem` 化）— 最小工数でモバイル実害を解消
2. **QA-001**（`components.css` 分割）— ユーザー承認後の構造変更
3. **QA-005**（`--text-muted` の引き上げ）
4. **QA-006 / QA-007 / QA-008**（ドキュメント是正）
5. QA-009〜QA-013（Low: ARIA付与、エラー処理、リセット挙動、チップ重複）

## 9. 対応案と承認状態

| 案 | 内容 | レベル |
|---|---|---|
| 案A（最小修正） | QA-002（1行）+ QA-007（仕様書末尾削除）のみ | C |
| 案B（推奨） | 案A + QA-001（CSS分割）+ QA-005 + QA-006 / QA-008（README・RECORD是正）。完了後にマイクロコミット＋再監査 | A/B |

**承認状態（2026-08-25時点）**
- ✅ 本報告書の作成・保存 → 承認済み（実施済み）
- ⏸ コード修正（案A / 案B のいずれか）→ 未承認（ユーザーの選択待ち）

## 10. 備考

- 前回監査（`AUDIT_REPORT_2026-08-25.md`）の QA-003（showToastタイマー）/ QA-004（自動テストなし）の判定は本監査でも妥当と確認。
- 本監査では `index.html` のインラインスタイル散在（111 / 115 / 119 / 125 / 163行目）、`main.main-content` の入れ子重複（29 / 46行目）、favicon未設置、CSPメタタグ未設定を Suggestion（任意改善）として把握済み。必須修正対象外。
- 本ファイルを本日のコードレビューの正本とし、既存の重複監査レポート（`audit_report.md` / `AUDIT_REPORT_2026-08-25.md`）の統合は QA-008 として別途承認の上で実施すること。


